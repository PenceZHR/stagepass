# 交接：接着做（2026-08-05 深夜场 · F 档全落）

> **这一份只放「这一趟发生了什么」**（分工见 `docs/BACKLOG.md` 开头）。
> 上一份是 `HANDOFF-2026-08-05c.md`（C 档）。这一趟按它排的顺序做了 **F 档**：
> 事件流、阶段图数据化、长回边、跳转栈。`pnpm check` **861 全绿**（净增 47 条）。

## 一句话

**状态机第一次有了回头路**：`sendBack` 把工作打回任一严格上游、`returnStack`
存得下嵌套回跳、账本带上了理由、跳转表是账本的投影 —— §5.5 反馈链路缺的两块
地基（§5.9.1/5.9.2）都在了，而且没有为此新建一张表。

```bash
pnpm check      # 861 测试 + 严格 typecheck
```

---

## 〇、现在停在哪

```
CHG-001      Build/blocked，24 条开着 —— 照旧没跑轮（停轮指令还在）
面板         用户那个还跑着（旧代码）。⚠ 见下面「重启前必读」
stagepass    这一趟已提交（见 git log）
```

### ⚠ 重启面板前必读

下一次起面板，`migrate()` 会**重建 `changes` 表**（return_phase → return_stack，
这棵树第一次整表迁移）。已经拿**真库的拷贝**离线验过：CHG-001 无损、74 行账本
原封、触发器当场重建、跑两次幂等（`schema.test.ts` 三条 + 手验记录在下面 §二·4）。
但**别在旧代码面板还开着的时候起新面板** —— 两个进程一个重建表一个在读，WAL
也救不了这种。先关旧的，再起新的。

---

## 一、这一趟做完的（BACKLOG §4.3/§4.5/§5.9.1/§5.9.2 是权威，这里只记证据）

### 1. 长回边 + 跳转栈（L0，最重的一块）

- 新动作 `sendBack(to)`：目标必须**严格上游**；`returnPhase` 单字段换成
  `returnStack` 栈，打回和 Review/QA 送修**统一成一个机制**，approve 弹栈回去。
  不变量钉死（栈序严格递减、Fix 顶必须是 Review/QA、closed 栈空）。
- 穷举证明跟着改：7 个动作 × 12 阶段 × 5 状态全判定（82 legal / 261 rejected /
  77 unrepresentable），嵌套回跳走的正是 §5.9.2 那个例子。
- **没留兼容层**：`returnPhase` 全树清零（零容忍那条规矩）。

### 2. 阶段图是值（§4.5）

`PhaseGraph`：默认 = 除 Fix 外的全序；自定义 = **子序列**（跳过可以，重排不行 ——
「上游」建立在全序上）。`projects.phase_order` 存，创建/转移/闸门都按图走。
设置它的 UI 没做（等 bootstrap 真造新项目）。

### 3. 事件流的判法（§4.3）——「账本就是它」

`change_events` 本来就是 append-only 事件流，**不另建表**。补了 `reason` 列和
投影 `domain/journey.ts#jumpsFrom`（从哪 · 去哪 · 方向 · 理由 · 哪一轮），
`/api/panel` 的 `journey` 字段带出去。真机投影：CHG-001 74 行账本 → 19 跳，
TestPlan/Build 各自环 3 次 —— §5.9.4「环在撒谎」第一次机器可读。

### 4. 第一次正经迁移（表重建）

旧 CHECK 绑死 return_phase，SQLite 改不了约束 → 官方十二步重建，行数不等就回滚，
触发器当场重建（等 SCHEMA_SQL 就有一段没人守账）。真库拷贝实测：无损。

### 5. 裁决表的「打回上游」

「打回去修」（代码错 → Fix）旁边多了「打回上游」（文档错 → 那个设计阶段）：
目标格 `T`（标签静态、名单动态）、第二趟理由 `Tx`。半个决定（选了打回没选哪份）
被拒并说出来（`no_target_chosen`），端到端两条集成测试盯着。

### 6. 棘轮又咬了两口，都咬对了

handle() 1462 → 加功能到 1466 → 抽 `askFollowUp`（第二趟追问整块出去）→ **1453**。
例外表两天从 1463 钉到 1453。教训记一条：别用行数杂技应付棘轮（我试过一次把 `});`
挤上行，当场改回来了）—— 它要的是拆出真函数。

---

## 二、还没做的（顺序照旧）

| | 是什么 | 在哪 |
|---|---|---|
| **G** | 环从「路径」变「地图」：读 `journey` 画历史箭头（实线/虚线、弦、自环、节点刻度） | §5.9.3 / §5.9.4 |
| **反馈闭环** | 打回理由送进**目标阶段**下一轮提示词（原文 + 收件人，走名单文件） | §5.9.1 那段「还没做的一半」 |
| **H** | 图谱机器 | §五 |
| **J** | 继续拆 handle()（棘轮在逼了） | §4.1 |

**真机待办**（都要跑轮/真点）：C 档题面（05c §三）、许可门解药（§2.1）、
E 窄提交、**这一趟的打回上游真机走一遍**（下一次任何阶段 settled 时裁决表里
应当有「打回上游」+ 目标格；P0 那格没有「先接受这个风险」）。

---

## 三、新东西在哪

```
src/domain/journey.ts        跳转表 = 账本投影（G 档只许读这一份）
src/domain/phase.ts          PhaseGraph / phaseGraphOf / upstreamOf / DEFAULT_GRAPH
src/domain/change-state.ts   sendBack / returnStack / TransitionOptions
src/db/schema.ts             CHANGES_TABLE_SQL、CHANGES_TRIGGERS_SQL、migrateReturnStack
src/web/panel-server.ts      askFollowUp（从 handle 抽出的第一块业务逻辑）
```
