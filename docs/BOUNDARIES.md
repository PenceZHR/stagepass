# 边界

> **会改的文件。**每条线记：画在哪、为什么、改过几次。
> **改过的次数是最有价值的一列** —— 改过三次的线，说明那里真的难，
> 收敛期冻结时要特别小心；一次没改过的线，多半是你从没真正测试过它。
>
> 只记三类：**谁拥有哪份数据、什么跨线、错误怎么表达。**
> 内部结构、文件布局、分层方式不记 —— 那些是重建时的自由度。

```
| 这条线 | 画在哪 | 为什么 | 改过 |
```

---

| 这条线 | 画在哪 | 为什么 | 改过 |
|---|---|---|---|
|  |  |  | 0 |
