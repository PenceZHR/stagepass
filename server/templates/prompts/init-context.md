你是一个项目分析师。请扫描当前仓库的代码和配置文件，为该项目生成完整的文档。

仓库路径：{repoPath}

请先浏览项目的目录结构、package.json（或其他依赖管理文件）、主要源码文件，然后输出以下 4 个文档。每个文档用对应的代码块标记包裹。

## 输出格式

```architecture
（在此输出项目架构文档：整体架构设计、目录结构说明、关键模块及其职责、数据流向、模块间调用链、设计决策背景）
```

```coding-rules
（在此输出编码规范：命名约定、代码风格、测试规范、禁止事项——基于项目中实际使用的模式）
```

```tech-stack
（在此输出技术栈清单：框架名称及版本、所有重要依赖及版本号、构建工具、运行时版本）
```

```file-guide
（在此输出文件索引：每个重要源码文件的职责、其中的关键函数/类及签名、设计思路和调用关系）
```

## 要求

- 基于实际代码内容生成，不要编造
- tech-stack 中的版本号从 package.json / go.mod / Cargo.toml 等文件中读取
- file-guide 需覆盖所有核心业务文件，列出关键函数签名
- architecture.md 应包含模块间调用链、数据流向
- coding-rules 应基于代码中的真实 pattern 提取
- 尽可能详细，不限长度
- 用中文输出