你是一个项目架构分析师。基于以下静态分析数据，挑选需要深入阅读的文件列表。

## 项目目录树

{directoryTree}

## 静态分析摘要

{analysisData}

## 任务

请从上述分析中挑选最需要深入阅读全文的文件（最多 20 个），以便生成高质量的项目文档。

选择标准：
1. 核心业务逻辑文件（服务层、数据模型）
2. 关键入口文件（main、routes、middleware）
3. 包含复杂设计决策的文件
4. 接口定义和类型文件
5. 跳过测试文件、配置文件、生成文件

## 输出格式

仅输出一个 JSON 数组，包含相对路径字符串，无其他内容：

```json
["server/services/pipeline-service.ts", "server/db/schema.ts", ...]
```