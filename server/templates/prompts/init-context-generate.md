你是一个高级项目分析师。请基于以下静态分析数据和深入阅读的源码内容，为项目生成完整的技术文档。

仓库路径：{repoPath}

## 静态分析数据

{analysisData}

## 深入阅读的文件内容

{fileContents}

## 输出要求

请生成以下 4 个文档。每个文档用对应的 XML 标签包裹。文档正文内部可以正常使用 Markdown 代码块，但不要在外层再使用 fenced code block 包裹整份文档。

### architecture.md 要求：
- 整体架构设计（前端/后端/数据库/AI 引擎等层次）
- 架构模式（MVC/微服务/单体等）
- 目录结构说明（每个顶级目录的职责）
- 模块间调用链和数据流向（用箭头描述）
- 关键设计决策及其背景（为什么选择某种方案）
- 状态机或生命周期描述（如果有）

### coding-rules.md 要求：
- 命名约定（基于实际代码中的 pattern）
- 代码风格（基于实际使用的库和模式）
- API 风格（路由约定、请求响应格式）
- 测试规范（使用的框架、组织方式）
- 禁止事项（从代码中发现的约束）

### tech-stack.md 要求：
- 所有核心依赖的名称和精确版本号（从 package.json 等提取）
- 按类别分组（框架、数据库、UI、工具等）
- 构建和运行时配置

### file-guide.md 要求：
- 覆盖所有核心业务文件
- 每个文件列出：职责、关键函数签名、设计思路
- 标注文件间的调用关系
- 重要的类和接口及其方法列表

## 输出格式

<architecture>
（架构文档内容）
</architecture>

<coding-rules>
（编码规范内容）
</coding-rules>

<tech-stack>
（技术栈内容）
</tech-stack>

<file-guide>
（文件索引内容）
</file-guide>

## 注意事项

- 基于实际代码和静态分析数据生成，不要编造不存在的内容
- file-guide 中的函数签名必须与静态分析数据一致
- 版本号从提供的分析数据中提取，不要猜测
- 尽可能详细，不限长度
- 用中文输出
