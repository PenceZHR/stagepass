# 这些产物来自端到端验证，不是本仓库的真实 PRD

`prd.md` 和 `prd.json` 是 2026-07-28 验证 Codex 流水线时生成的。

当时的测试项目 `stagepass-gateway-e2e` (PRJ-001) 把 **本仓库自己** 当作工作区，
所以模型读的是这里的代码，产物也写回了这里的 `.ship/`。内容是真的——模型确实
读过仓库——但它描述的是"如果要重做 StagePass 会怎么设计"，而不是本仓库当前
的产品定义。

流水线状态记在 `/private/tmp/stagepass-codex-e2e/ship.db`，不在这个仓库里。

如果 StagePass 要为自己写一份真的 PRD，先删掉这两个文件再走一遍流程，
否则模型会把这份草稿当成既有事实继续往上叠。
