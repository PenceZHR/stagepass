{
  "name": "stagepass",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "test": "node --import tsx --test --test-concurrency=1 \"src/**/*.test.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.src.json && tsc --noEmit -p tsconfig.panel.json",
    "check": "pnpm typecheck && pnpm test",
    "schema:app-server": "codex app-server generate-json-schema --experimental --out docs/app-server-schema",
    "stagepass": "tsx scripts/stagepass.ts",
    "plugin": "tsx scripts/plugin-build.ts",
    "preview": "tsx scripts/plugin-preview.ts"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20",
    "esbuild": "^0.28.2",
    "tsx": "^4.22.4",
    "typescript": "^5.9.3"
  }
}
