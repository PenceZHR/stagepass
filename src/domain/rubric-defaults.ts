import type { Phase } from "./phase";
import type { CriterionDraft, RubricRole } from "./rubric";

/**
 * 出厂自带的标准。**一条都不阻断。**
 *
 * ## 为什么全是 `blocking: false`
 *
 * `not_assessed` 是阻断的（标了阻断的漏答视同阻断）。所以出厂就勾上阻断，等于
 * **任何一次模型漏答都会立刻给每一个项目挂上一条挡门的东西** —— 而它的出口只有
 * 「进设置里把这条标准撤下来」。人会在完全不知道 rubric 是什么的情况下先被拦住。
 *
 * 默认是给人一个起点去读、去改、去决定哪几条值得挡，不是替他做那个决定。
 * 这条是 `RUBRIC-DESIGN.md` §3 末句，别在补默认条目时顺手把它翻掉。
 *
 * ## 为什么 critic 和 verdict 只有一套
 *
 * 它们讲的不是产物，是**方法**：怎么质疑一份东西、怎么裁一场争论。这两件事在
 * PRD 和在 Merge 是一样的。给每个阶段各抄一份只会得到十一份同样的话，然后它们
 * 开始各自漂移 —— 那正是「一个概念一个名字」要挡的。
 *
 * 只有 producer 是逐阶段的，因为「这个阶段该产出什么」逐阶段不同。
 *
 * ## 这些话是起点，不是权威
 *
 * 它们没有经过用户拍板。真正的权威是用户在面板上改出来的那一版 —— 这里只保证
 * 一个新项目不是从空白开始。
 *
 * ## 这个模块是纯的
 */

/**
 * 怎么质疑。和阶段无关。
 *
 * **「和阶段无关」是一条约束，不只是一句描述**：这一份所有阶段共用，所以里面任何
 * 一句绝对化的话都必须在每一个阶段都成立。
 *
 * 2026-07-30 抓到过一次反例：第三条原来写「没有提出需要读仓库或跑代码才能验证的
 * 问题 —— 只基于摆在面前的产出」。那句话在设计阶段是对的，但 Build 和 Review 的
 * 蓝方**明确被允许读代码**（`domain/round.ts` 的 `blueReach`）—— 裁判会拿这条把
 * 蓝方最值钱的那类发现判成违规，正好把刚开的权限抵消掉。
 *
 * 现在那条改成「建立在它实际看过的东西上」：**够得着多少是各阶段的事，不靠猜是
 * 所有阶段共同的事** —— 后者才是这一份该管的。
 */
const CRITIC = [
  "每条问题都指向产出里的具体位置，而不是泛泛地说「不够清楚」",
  "每条问题都说明了它为什么是问题，而不只是说它存在",
  "每条问题都建立在它实际看过的东西上，没有靠猜、也没有假设一件它没核实过的事",
  "同一个问题在后续轮次沿用了同一个 id，没有换个说法重开一条",
];

/**
 * 怎么裁。和阶段无关。
 *
 * 第三条 2026-07-31 重写过。原来是「没有替正方复述或辩解 —— **裁决只针对问题本身**」，
 * 那句是照着「裁判只做一件事」写的。用户当天定下裁判还要给一个「还要不要再来一轮」
 * 的结论，并且**要自己去读上游文档和两方产出**才下得了这个结论 —— 旧措辞会把它现在
 * 该做的事判成越界。
 *
 * 和 2026-07-30 `CRITIC` 第三条那次是同一个病：一条写死的禁令，在权限放开之后正好
 * 把最值钱的那类动作判成违规。**放开一样能力，就要回头看有没有哪条标准在禁它。**
 *
 * 新加的第五条对着那个结论：它是给人看的建议，但「有没有给」和「有没有依据」是人
 * 要看的两件事。
 */
const VERDICT = [
  "对每一条已经存在的问题都明确表了态，没有沉默略过任何一条",
  "关闭任何一条时都写清楚了它为什么不再成立，而不是「已修复」四个字",
  "结论建立在它自己读过的上游文档和两方产出上，而不是复述正方或反方说过的话",
  "没有把「这一轮没提到」当成「已经解决」",
  "给出了「还要不要再来一轮」的结论，并写清楚依据是什么",
];

/** 每个阶段该产出什么。**这一栏才是逐阶段不同的。** */
const PRODUCER: Readonly<Record<Phase, readonly string[]>> = {
  PRD: [
    "写清楚了要解决谁的什么问题，而不是先写要做什么功能",
    "每条需求都有可观察、可测量的验收标准",
    "明确写出了这一次**不做**什么",
    "列出了至少一个会让整个方案不成立的前提",
  ],
  Spec: [
    "每条需求都能对应回 PRD 里的某一条，没有凭空多出来的",
    "每条行为都写了它的边界情况和出错时的表现",
    "用词和上游文档一致，没有为同一个概念发明第二个名字",
    "标明了哪些地方是有意留待 TechSpec 决定的",
  ],
  TechSpec: [
    "写清楚了数据怎么存、状态怎么迁移，而不只是模块怎么划分",
    "每一个技术选择都写了它的代价，不只是好处",
    "指出了这个设计里最可能出错的一处，以及为什么",
    "没有引入 Spec 里不存在的行为",
  ],
  Plan: [
    "每一步都能独立验证，不需要等到最后才知道对不对",
    "步骤之间的依赖是明确的，没有两步在改同一处却没定顺序",
    "写了出问题时怎么退回去",
    "标出了哪一步风险最高，以及为什么先做或后做",
  ],
  TestPlan: [
    "每条验收标准都至少有一个用例对着它",
    "写了失败长什么样，而不只是成功长什么样",
    "区分了「必须通过」和「知道会失败但先记着」",
    "没有把「跑一遍看看」当成一条用例",
  ],
  /*
   * Build 这几条是照着**蓝方够得着什么**写的（2026-07-30 定）：它能读这一轮改动
   * 涉及的文件和它们的直接调用方，但**不自己执行**。
   *
   * 所以原来那条「自己跑过一遍，而不是只保证能编译」被换掉了 —— 判它的人跑不了
   * 东西，那条标准只能靠猜，而一条只能靠猜的标准比没有更糟：它每一轮都会得到一个
   * 没有依据的 yes。换成「交出运行证据」，判据就落在**看得见的东西**上。
   *
   * 「受影响的调用点都跟着改了」这条是蓝方那个新权限换来的：它是审代码里最值钱的
   * 那类问题，而只看 diff 的人永远问不出来。
   *
   * ## 2026-08-04：主轴换成编码规范
   *
   * 用户定的：这个阶段该判的是**编码一致性、可读性、可维护性，以及和上游文档的
   * 语义对齐**。原来那五条偏「有没有做完」，判的是完成度不是质量。
   *
   * 新加的四条仍然守着同一条老规矩 —— **只写蓝方够得着的**：
   *
   *   命名对齐上游    任务里列出了已批准的上游产物，它读得到，对得起来
   *   行为对齐 Spec    同上
   *   风格一致        「它周围和它的直接调用方」正好是蓝方的可达范围
   *   一处定义        同一份逻辑抄成两份，在改动文件加直接调用方里看得出来
   *
   * **没有写「和整个仓库的风格一致」**：蓝方被明确要求不要通读整个仓库，那条只能
   * 靠猜 —— 和 2026-07-30 撤掉「自己跑过一遍」是同一个判据。
   *
   * **合并被否掉了。** 曾想把「导出有调用方」和「调用点都改了」并成一条，
   * 复合标准判不出干净的 yes/no（前半成立、后半不成立时判什么？），所以拆着放。
   */
  Build: [
    "改动范围和 Plan 一致，没有顺手做计划外的事",
    "命名和上游文档一致：Spec / TechSpec 里叫什么，代码里就叫什么，没有为同一个概念在代码里发明第二个名字",
    "实现的行为对得上 Spec 写的那几条；多做或少做的地方在代码或报告里写明了为什么",
    "新写的代码和它周围、以及它的直接调用方风格一致：命名、错误处理、文件位置都跟着现有做法，没有在同一个仓库里另起一套",
    "每一处不明显的决定都在代码里留下了为什么",
    "同一条规则只有一个定义处，没有把同一份逻辑抄成第二份",
    "新增或改名的每个导出都有调用方，没有留下没人调用的代码或半途的开关",
    "受影响的调用点都跟着改了，没有把现成的调用方留在对不上的状态",
    "交出了运行证据：跑了哪条命令、输出是什么，而不是只说「应该能跑」",
  ],
  /*
   * Review 这几条也是照着**判它的人够得着什么**写的：蓝方能读被审的那个 commit
   * 涉及的文件和它们的直接调用方，但不自己执行（2026-07-30 定）。
   *
   * 加的第一条是「写清审的是哪个 commit」—— 审 A 不等于审 B，而这份报告会被下一轮
   * 和下一个阶段当成事实引用。少了它，一份意见挂在空气里。
   */
  Review: [
    "写清楚审的是哪一个 commit，而不是笼统地说「当前代码」",
    "逐条对照了 Spec 和 TechSpec，而不是通读一遍谈感受",
    "每条意见都指明了文件和位置，看的人能直接翻到那儿",
    "区分了「必须改」和「可以这样也可以那样」",
    "检查了错误路径和边界，而不只是主流程",
  ],
  Fix: [
    "每一条修的都对应一条具体的问题，没有夹带",
    "说明了为什么这么修能让那条问题不再成立",
    "没有为了让检查通过而绕开检查",
    "改完之后原来的用例仍然成立",
  ],
  /*
   * QA 这几条判得了，是因为**反方在这一阶段可以自己跑**（domain/round.ts 的
   * `blueReach`）—— 「按 TestPlan 逐条执行了」和「别人能重现」这两条，只有一个跑得动
   * 东西的人核对得了。在 Build 那边同样的话就只能靠猜，所以那边写的是「交出运行证据」。
   */
  QA: [
    "写清楚测的是哪一个 commit，而不是笼统地说「当前代码」",
    "按 TestPlan 逐条执行了，没有跳过，跳过的写明了为什么",
    "失败的用例记下了实际输出和它来自哪一条用例，而不只是「没过」",
    "确认过这一轮的改动没有让别处退化",
    "跑的命令和环境写下来了，别人照着能重现",
  ],
  /*
   * 原来第一条是「没有未解决的阻断项」—— **那是闸门在管的事**（`domain/gate.ts` 会拿
   * `blocking_problem_outstanding` 直接拒），让模型再判一遍只会得到一个没有依据的
   * yes：它判「有」的时候闸门已经拒了，它判「没有」的时候闸门也不看它。
   * 同一件事两处判，而其中一处只能靠猜（用户 2026-07-30 拍板撤掉）。
   *
   * 换上来的都是**只有看过东西才答得出**的 —— 而蓝方在这一阶段读得到这一次的全部
   * commit，所以它们判得了。
   */
  Merge: [
    "每一条被接受的风险都写了理由和影响范围，而不是默默放行",
    "变更说明写清楚了这次改了什么、对谁有影响，而不是复述阶段名",
    "写明了怎么回滚，而不只是说「可以回滚」",
    "说明和实际改动对得上，没有漏掉某个 commit 里的东西",
  ],
  Retro: [
    "写的是这一次真实发生的事，不是应该发生的事",
    "至少有一条是关于流程本身的，而不全是关于代码",
    "每条结论都能落到一个具体的下次会不一样的做法",
    "记下了这次做对的事，不只是做错的",
  ],
  // 终局阶段：没有 turn 在这里跑，所以没有人可以被摆一张清单。
  // 硬凑一份出来，就是给一个后端答不出的栏目 —— 那是反着方向的静默失效。
  Done: [],
};

/**
 * 个别阶段要多判一条。**这没有翻掉「critic / verdict 只有一套」那条。**
 *
 * 那条挡的是「把同样的话抄十一份，然后各自漂移」。这里是**追加**：共用那套仍然是
 * 唯一一份、仍然逐字共用，补充的部分只在指名的那个阶段出现，两处不重复。
 *
 * 判据是**方法还是内容**：
 *
 *   「每条问题都指向具体位置」        方法 —— 在 PRD 和在 Merge 一样成立 → 共用
 *   「也看了风格和命名对不对得上」    内容 —— 放到 PRD 上就是一句空话   → 按阶段
 *
 * Build 这一条 2026-08-04 加的，起因是同一条老规矩又被踩了一次：那一轮给 Build 的
 * producer 加了四条编码规范，也给反方的任务加了「去挑编码上的毛病」，**却没有人判
 * 反方到底挑没挑** —— 要求和判据又对不上了（见 `codex/turn-runner.ts` 里那段）。
 */
const CRITIC_EXTRA: Partial<Readonly<Record<Phase, readonly string[]>>> = {
  Build: [
    "除了功能对不对，也看了编码上的毛病：风格和周围的代码对不对得上、命名和上游文档对不对得上、同一条规则有没有被抄成两份",
  ],
};

/**
 * 同上，裁判那一侧。
 *
 * Build 这一条防的是一个具体的失效：编码规范类的问题**天生看起来「不影响功能」**，
 * 而裁判手里正好有一个「判它不成立」的按钮。让它这么关掉，等于这四条标准写了也白写。
 *
 * **它的正确出口是人接受风险（waive），不是裁判判它不成立** —— 前者留在交付说明里，
 * 后者在库里什么都不留。
 */
const VERDICT_EXTRA: Partial<Readonly<Record<Phase, readonly string[]>>> = {
  Build: [
    "没有因为「不影响功能」就把编码规范类的问题关掉 —— 那类问题的出口是人接受风险，不是裁判判它不成立",
  ],
};

/**
 * 一个阶段、一个角色的出厂标准。
 *
 * 返回空数组是合法的，意思是这一栏没有出厂默认 —— 空 rubric 本来就合法，
 * 等于这个角色不做判定。
 */
export function defaultCriteria(phase: Phase, role: RubricRole): CriterionDraft[] {
  const texts = role === "producer" ? PRODUCER[phase]
    : PRODUCER[phase].length === 0 ? []
      : role === "critic" ? [...CRITIC, ...CRITIC_EXTRA[phase] ?? []]
        : [...VERDICT, ...VERDICT_EXTRA[phase] ?? []];
  // blocking 一律 false，理由见文件开头。**不要在这里开一个参数把它打开。**
  return texts.map((text) => ({ text, blocking: false }));
}
