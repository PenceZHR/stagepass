import type { Phase } from "./phase";

/**
 * 一个阶段的产出模板 —— **把散文变成离散的**。
 *
 * ## 为什么要有它
 *
 * `BACKLOG.md` §5.6 已经推过这条理由，只是当时只用在代码上：**文档是连续的，改一处
 * 文字就产生新的可挑剔表面**；模块是离散的，可挑的表面小到能被穷尽。
 *
 * 真库取证（`~/.stagepass/panel.db` 的 CHG-001，21 轮跑完）：设计阶段每轮开 2~8 条，
 * **没有一个真正收敛**，全靠人 waive 才过闸门。PRD 四轮是同一句抱怨在降尺度，
 * 第四轮已经追到「13.0 的词法形式」。红方的隐含标准是「每个词都能唯一裁定」，
 * **任何有限文档都不满足它**，所以它能永远产出 4~5 条。
 *
 * 模板把无限表面切成有限个格子：格子有限 → 可挑表面有限 → **这才第一次有收敛点**。
 * 用户 2026-08-06：「必须照着模板来填，否则如果让模型自己发挥的话，是永远没法收敛的。」
 *
 * ## 和 `phase-play.ts` 同一条纪律
 *
 * 每个阶段各写各的，**不许提取公因子**。提取出来就是把模板悄悄请了回来，而下一次
 * 「只想改 PRD 那一节」又会变成往公共件上挂分支。
 *
 * ## 十个阶段有，只有 Fix 和 Done 没有
 *
 * 用户 2026-08-06：「覆盖到所有 stage。」原来止于「产出文档的阶段」，把 Build
 * 挡在外面 —— **而那个判据是错的**：Build 拿不到文档路径不是因为它交 commit，
 * 是因为 `round-turn-runner` 拿 `producesCommit` 顺手决定了「给不给路径」，
 * 把两件事绑成了互斥。解开之后 Build 交两样：代码进 commit，施工报告进模板。
 *
 * Fix 暂时不给：它是「改掉被报出来的那几条」，一次可能只动两行，套一份报告
 * 负担大于收益。**等 Build 这一份真跑过再决定要不要复制** —— 和当初
 * 「先只做 PRD 一个」同一条纪律。
 *
 * 没有模板的阶段返回 `null`，调用方按「照旧」走，**不是按「空模板」走**：
 * 空模板会让红方收到一份零节的清单然后什么都不写。
 *
 * ## 这个模块是纯的，只 import 一个类型
 */
export interface TemplateSection {
  /** 跨版本稳定。rubric 的 criterion 挂在它上面（`domain/rubric.ts` 的 `section`）。 */
  readonly key: string;
  /** 红方要写的那个标题，**也是认节的依据**。 */
  readonly title: string;
  /** 这一节要回答什么。 */
  readonly asks: string;
}

/**
 * PRD 的六节。
 *
 * 内容是**起点不是权威** —— 和 `rubric-defaults.ts` 同一句话。真正的权威是用户改
 * 出来的那一版。
 *
 * `deferred` 那一节是承重的：它给「这件事属于下游」一个**红方声明**的位置。没有它，
 * 「架构没定」这种话除了开成 gap 无处可去 —— 真库里 PRD 那 18 条越界（追 TechSpec 的
 * 规范化对象、TestPlan 的失败阈值、Plan 的工具冻结版本）正是这么来的。
 */
const PRD_SECTIONS: readonly TemplateSection[] = [
  {
    key: "problem",
    title: "要解决谁的什么问题",
    asks: "谁在用、他今天怎么受阻、不解决会怎样。不要写要做什么功能。**用的人要点得出"
      + "名字**：哪一类人、在什么场景下，不是一句「用户」；怎么受阻要写出他今天实际走的"
      + "那几步、卡在哪一步，不是「体验不好」。",
  },
  {
    key: "outcome",
    title: "做完之后什么变了",
    asks: "可观察的结果，不是功能清单。每条写清谁、在什么时候、看到什么不一样了；"
      + "和今天比是从什么变成什么 —— 能写出数的地方写数，「更快」「更稳」后面必须跟着"
      + "从多少到多少。",
  },
  {
    key: "acceptance",
    title: "验收标准",
    asks: "每条可观察、可测量 —— 到「一个称职的实施者照着做不会做错」为止，"
      + "不要求每个词都能唯一裁定。但**下限是三样都写出来**：在什么条件下、量的是哪个"
      + "指标、到什么数（或什么状态算通过）。这两句不冲突：前一句说的是不必再往下抠词，"
      + "这一句说的是这三样一样都不能少。",
  },
  {
    key: "out-of-scope",
    title: "这次不做什么",
    asks: "明确排除掉的。每条点到具体的那件事，不是一个大类 ——「暂不做性能优化」"
      + "不算一条，要写出不做的是哪个场景下的哪件事，以及不做它谁会受影响。",
  },
  {
    key: "assumption",
    title: "前提",
    asks: "至少一条会让整个方案不成立的前提，以及怎么判它成不成立。判法要写成**一个"
      + "做得出来的动作加一条明确的分界**：谁去做什么、看到什么就算它不成立 ——"
      + "「需要进一步验证」不是判法。",
  },
  {
    key: "fixed",
    title: "已经定死的约束",
    asks: "人已经拍死、这次不容商量的：技术栈、平台、必须兼容什么、不许动什么。"
      + "**只转述，不新定** —— 这里写的每一条都要说得出是谁定的。没有就写「没有」。"
      + "每条要点到名字和版本：哪个框架、哪个版本、哪个平台、哪个不许动的文件或接口"
      + "——「沿用现有技术栈」没说出任何一个名字，下游照样得猜。",
  },
  {
    key: "deferred",
    title: "留给下游决定的",
    asks: "有意留给下游去定的，写明留给哪个阶段。架构、模块划分、接口、测试用例、"
      + "实现步骤都不在 PRD 里定；**已经被人定死的不写这儿，写上一节**。每条除了写"
      + "留给哪个阶段，还要把**要它答的那个问题**写出来 ——「架构留给 Arch」不算一条，"
      + "「A 和 B 要不要合成一个模块，留给 Arch」才是。",
  },
];

/*
 * `fixed` 那一节是 2026-08-06 真机撞出来的。
 *
 * 第 3 轮和第 5 轮，`deferred` 那条标准**连着两轮判 no，而且红方两轮都改了**：
 * 它先把「Cocos Creator 3.8.8」写在结果和验收里（判 no），再挪进「留给下游决定的」
 * （还是判 no —— 一个不可退让的约束根本不是「留给谁决定」）。
 *
 * **红方赢不了，因为六节里没有一节放得下「人已经定死的东西」。** 那不是模型的问题，
 * 是模板漏了一格 —— 和 `rubric-defaults.ts` 里那句「一条只能靠猜的标准比没有更糟」
 * 同一个形状：一条**结构上满足不了**的标准，比没有更糟。
 */


/**
 * Spec 的五节。消费 PRD（`CONSUMES`）。
 *
 * `scope` 排第一是因为 Spec 唯一的立身之本是**它回答的是 PRD 提的那些问题** ——
 * 一份对不回上游的 Spec，后面每个阶段都在给一个没人要过的东西做实现。
 */
const SPEC_SECTIONS: readonly TemplateSection[] = [
  {
    key: "scope",
    title: "这份 Spec 覆盖 PRD 的哪几条",
    asks: "逐条对应回 PRD，写清哪条验收标准由这里的哪几段行为满足。没有凭空多出来的。"
      + "**两边都要点到名**：PRD 那一条写出它的编号或原话，这里这几条写出它们的名字或"
      + "编号 ——「已覆盖 PRD 的验收标准」不是一条对应关系，它两边都没点到。",
  },
  {
    key: "behaviour",
    title: "行为",
    asks: "每条行为的正常路径：给什么、做什么、出什么。**每条行为要有一个名字或编号**，"
      + "下游靠它引用；输入逐项列（每项叫什么、什么含义、必填还是可选、取什么值算合法），"
      + "输出写出具体是什么 ——「处理请求并返回结果」一项都没交代。说的是**行为层面的"
      + "输入输出，不是函数签名** —— 那些属于 Arch。",
  },
  {
    key: "edge",
    title: "边界和出错",
    asks: "每条行为在边界上和出错时的表现。没有「其余情况未定义」这种写法。每条要写出"
      + "**触发条件**（哪个输入、取什么值、在什么状态下）和**当时的具体表现**（出什么、"
      + "报哪一种错、做了一半的怎么办）——「报错」不是表现，它没说是哪一种错。",
  },
  {
    key: "terms",
    title: "用词",
    asks: "这份文档里的关键词各是什么意思。和上游一致，同一个概念不发明第二个名字。"
      + "每个词要说得出**什么算、什么不算**，而不是把这个词换个说法再说一遍；和 PRD "
      + "用词不同的，写出它对的是 PRD 里的哪个词。",
  },
  {
    key: "deferred",
    title: "留给下游决定的",
    asks: "有意不在 Spec 定的，写明留给哪个阶段。数据怎么存、模块怎么划分、"
      + "用什么技术都不在 Spec 里定 —— 需要提到就写在这一节。每条要把**要下游答的"
      + "那个问题**写出来 ——「存储方案留给 Arch」不算，「这份状态要不要落盘、落在哪儿，"
      + "留给 Arch」才是。",
  },
];

/**
 * Arch 的八节 —— **这一份就是完整的技术架构**（2026-08-08 用户拍：TechSpec 并进来）。
 *
 * ## 为什么合并
 *
 * TechSpec 和 Arch 是同一批决定的粗细两版：Arch 的「新增哪些依赖边」「边界露
 * 什么」就是 TechSpec 的「谁调谁、传什么、返回什么」，Arch 的「想过但没选的划法」
 * 就是 TechSpec 的「选择和它的代价」。真正只属于 TechSpec 的只有「数据怎么存」，
 * 撑不起一个独立阶段。而两份文档讲同一批决定的两个粒度**必然漂移** ——
 * 2026-08-08 真机证据：Arch 那份产出里出现七次「留给 TechSpec」，人读完拿不到
 * 完整图景。见 `domain/phase.ts` 的 `RETIRED_PHASES`。
 *
 * ## 为什么每一节都写着粒度下限
 *
 * 用户 2026-08-08：「所有流程输出内容都有点宽泛，我的目标是一个彻底的完整的
 * 项目架构，**精细到一个文件的函数和模块**。」
 *
 * 老模板问的是「动哪几个**模块**」—— 模型给的正是被问的东西。**问到哪一层，
 * 就只会答到哪一层**，这不是模型偷懒。所以每一节都写死了要写出什么名字、
 * 什么路径；反方那边有配套的挡门标准（`rubric-defaults.ts`），判据是
 * 「有没有一处只写了概括而没写出具体的名字或路径」—— 那是反方指得出来的，
 * 而「不够详细」不是。
 *
 * `edges` 那一节仍然是 BACKLOG §5.5 那条机械分界线的落点：「修一个模块时要新增
 * 一条图上没有的依赖边，就不是模块问题，是架构问题 —— 停手，攒进架构那一批。」
 * 「架构在脑子里」对 AI 不成立：写不下来就不存在。
 */
const ARCH_SECTIONS: readonly TemplateSection[] = [
  {
    key: "modules",
    title: "这次动哪几个模块",
    asks: "已有的哪几个、新增哪几个。**每个模块写出它的目录或文件路径**，"
      + "不能只给一个概念名字。每个新增的写清它为什么不能并进已有的。",
  },
  {
    key: "files",
    title: "文件与函数清单",
    asks: "**逐个文件列**：路径、这个文件负责什么、它导出哪几个函数或类型"
      + "（**把名字写出来**）、谁调它们。新建的和要改的都列，改的写清改哪几个"
      + "导出。这一节不许出现「等实现时再定」—— 定不下来的函数说明模块还没划清。",
  },
  {
    key: "data",
    title: "数据怎么存、状态怎么迁移",
    asks: "**每个数据结构写出它的字段名和类型**；哪些是权威、哪些是镜像；"
      + "状态之间怎么走（写出状态名和触发它的那个函数）。没有持久化数据时"
      + "明写「没有」，而不是跳过这一节。",
  },
  {
    key: "interfaces",
    title: "接口与契约",
    asks: "**逐个写出上一节点过名的函数的签名**：参数名和类型、返回什么、"
      + "出错时返回什么或抛什么。跨模块调用的每一条都要有；模块内部的私有函数"
      + "不用写。「返回一个结果对象」这种不算 —— 写出它有哪几个字段。",
  },
  {
    key: "edges",
    title: "新增了哪些依赖边",
    asks: "**每条写成「哪个文件 import 哪个文件」**，再写为什么非加不可 ——"
      + "说得出少了它哪个行为做不成。没有新增时明写「没有」：这一节是 Build "
      + "反馈回流的落点，空着和「没有」是两回事。",
  },
  {
    key: "boundaries",
    title: "边界划在哪",
    asks: "每个被动到的模块对外露什么、不露什么 —— **露的那些要点名到导出的"
      + "函数或类型**，不能只说「只露出配置能力」。用 Spec 的词，不发明新名字。",
  },
  {
    key: "alternatives",
    title: "想过但没选的划法，和选了的代价",
    asks: "至少一个没选的划法，写为什么没选（没有备选说明还没想）；**以及选了"
      + "的这一套要付的代价** —— 一个只有好处的选择说明代价还没被看见。",
  },
  {
    key: "riskiest",
    title: "最可能出错的一处",
    asks: "**指名一处**，写为什么是它、错了会怎样。并列罗列多项风险不算回答了"
      + "这一节 —— 那等于没有判断。",
  },
  {
    key: "traceability",
    title: "对得回 Spec",
    asks: "这里的每一组决定对应 Spec 的哪一条。**没有引入 Spec 里不存在的行为**；"
      + "真需要新行为，说明它是 Spec 漏了（那要打回 Spec），不是在这里补。",
  },
  {
    key: "deferred",
    title: "留给下游决定的",
    asks: "有意留给 Plan 或 Build 的，写明留给哪个阶段、以及为什么现在定不了。"
      + "**「实现细节」不是理由** —— 函数名、签名、数据结构都属于这一份，"
      + "推给下游的只能是「怎么排步骤」和「具体怎么写代码」。",
  },
];

/** Plan 的五节。消费 Arch（2026-08-08 起 TechSpec 并进了 Arch）。 */
const PLAN_SECTIONS: readonly TemplateSection[] = [
  {
    key: "steps",
    title: "步骤",
    asks: "每一步一个能独立交付的东西。一步做完就能看出对不对。**每一步要写出它动"
      + "哪几个文件**（路径写全，新建的写清准备放在哪儿），以及在每个文件里加什么、"
      + "改什么（函数名、导出名、配置项名）——「实现 X 模块」不是一步，它没说出一个"
      + "文件。确实不动文件的步骤（比如只跑一次验证）写清它跑什么、看什么。",
  },
  {
    key: "order",
    title: "依赖和顺序",
    asks: "哪几步必须先后，为什么。没有两步在改同一处却没定顺序。每条依赖写成「哪一步"
      + "在哪一步之前」（用步骤号），理由要点到**它们共用的那个东西**：哪个文件、哪个"
      + "函数、哪张表 ——「有依赖关系」不是理由，它没说共用的是什么。",
  },
  {
    key: "verification",
    title: "每一步怎么验",
    asks: "每一步各自的判据，不要等到最后才知道对不对。每一步写出**跑哪条命令**"
      + "（原样写出来）、**看到什么算过**；不是命令能验的，写出去看哪个文件的哪一处、"
      + "看到什么算过 ——「跑一下测试」「确认功能正常」都不是判据，它们没有对象。",
  },
  {
    key: "rollback",
    title: "出问题怎么退",
    asks: "实施到一半失败了怎么退回去。只写停止条件不算回答了这一节。要写出**做哪几个"
      + "动作**：退哪个 commit 或哪几个文件、已经改掉的数据要不要反着改回来、退完之后"
      + "看什么确认退干净了 —— 少了最后一样，退没退成没人知道。",
  },
  {
    key: "riskiest",
    title: "哪一步风险最高",
    asks: "指名一步，以及为什么先做或后做它。指的是**第几步**（写出步骤号或标题），"
      + "理由落到那一步动的那个具体东西上：哪个文件、哪条依赖、哪个还没验过的假设 ——"
      + "「涉及核心逻辑」没有指到任何一处。",
  },
];

/**
 * TestPlan 的五节。消费 TechSpec。
 *
 * ## `ran`（实际跑出来的结果）那一节 2026-08-06 加、当天又撤了
 *
 * §8.7·1 用户拍了：「肯定是 build 做完了跑 test」—— **TestPlan 只交测试方案和
 * 测试代码，自己不跑**；执行的责任在 Build 的蓝方，时点在红方写完之后。
 * `ran` 是按「TestPlan 跑在 Build 之前」做的，和这个分工冲突，所以撤掉，
 * `ran` 那两条出厂标准跟着撤（rubric-defaults）。TestPlan 留在
 * `PRODUCES_COMMIT` —— 它交代码，那件事和「先跑一遍」无关。
 */
const TESTPLAN_SECTIONS: readonly TemplateSection[] = [
  {
    key: "coverage",
    title: "验收标准到用例的映射",
    asks: "上游每条验收标准对应这里的哪几个用例。逐条列，不留空。**两边都点到名**："
      + "验收标准写出它的编号或原话，用例写出它的 id ——「已全部覆盖」不是一条映射，"
      + "它一个 id 都没写出来。",
  },
  {
    key: "cases",
    title: "用例",
    asks: "每个用例：给什么输入、在什么前置状态下、期望什么输出。**每个用例要有一个 "
      + "id，还要写出它落在哪个测试文件的哪个用例名上** —— 你交的是测试代码，下游按 id "
      + "单独跑它、按文件名翻到它。输入写出实际的值，不是「一个合法的输入」；前置状态"
      + "写出具体有哪些数据、系统在哪个状态；期望输出写到能逐字比对 ——「返回成功」"
      + "没说要比对什么。",
  },
  {
    key: "failure",
    title: "失败长什么样",
    asks: "不只写成功。每个用例失败时看到的是什么，怎么和「测试自己写错了」区分开。"
      + "**失败要写到能对上号**：报出来的是哪一句、断言差在哪两个值之间；区分方法要写成"
      + "一个做得出来的动作（跑哪条命令、改哪一处再跑一次看什么变了），不是"
      + "「检查测试代码」。",
  },
  {
    key: "gating",
    title: "哪些必须通过",
    asks: "区分「必须通过」和「知道会失败但先记着」，后者写明为什么先记着。**两边都按"
      + "用例 id 逐个列**，不是「核心用例都要过」；先记着的每条还要写出什么条件下它会被"
      + "解除 —— 没有解除条件的「先记着」就是永远记着。",
  },
  {
    key: "how",
    title: "怎么跑",
    asks: "命令、环境、前置条件。**跑的人不是你** —— Build 的蓝方在红方写完之后"
      + "照这一节执行，所以它必须细到不在场的人照着能跑。没有「跑一遍看看」这种写法。"
      + "命令**原样写出来能直接粘**（连同在哪个目录下跑），环境写到版本号（运行时、"
      + "包管理器、要不要先装依赖），并且写出**只跑其中一条用例**的命令 —— 反方是按 id "
      + "复核的，它得单独跑得动某一条。",
  },
];

/*
 * REVIEW_SECTIONS 删掉了（环 v3 把 Review 收编进 QA，2026-08-09）：它「逐条对照
 * 上游」「发现的问题」那几节的活儿，由 QA 的「读」那一攻接手 —— 批 5 定稿 QA
 * 模板时把静态审查的节补进 QA_SECTIONS。退休阶段不留模板（TechSpec 先例）。
 */

/** QA 的五节。消费 Build 和 Test（两轨对撞点；批 5 接案卷时定稿）。 */
const QA_SECTIONS: readonly TemplateSection[] = [
  {
    key: "subject",
    title: "测的是哪一个 commit",
    asks: "写出 sha。不要笼统地说「当前代码」。**sha 至少写到前 12 位**，再写出你是在"
      + "哪个目录、哪个分支、什么环境下测的 —— 反方要在同一处跑一遍核对你说的。",
  },
  {
    key: "executed",
    title: "按 TestPlan 逐条执行的结果",
    asks: "一条都不跳。跳过的写明为什么。**按 TestPlan 的用例 id 逐条列**，每条写清"
      + "跑的是哪条命令、结果是过还是没过 ——「全部用例执行通过」不是逐条，它一个 id "
      + "都没写出来。",
  },
  {
    key: "failures",
    title: "失败的用例",
    asks: "实际输出是什么、它来自哪一条用例。不只写「没过」。**输出贴原文**（报错那"
      + "几行、断言里实际值和期望值各是什么），并写出它的用例 id 和所在的测试文件 ——"
      + "转述一遍，下一轮就没法拿它比对了。",
  },
  {
    key: "regression",
    title: "有没有让别处退化",
    asks: "这一轮的改动之外的地方，查过没有、结果是什么。**写出查的是哪几处**："
      + "跑了哪条命令、覆盖了哪些用例或哪些文件，结果各是什么 ——「未发现回归」没说"
      + "查过什么，和没查看起来一模一样。",
  },
  {
    key: "repro",
    title: "怎么重现",
    asks: "跑的命令和环境写下来，别人照着能重现。**命令原样能粘**（连同在哪个目录下"
      + "跑），环境写到版本号，从零开始要做的那几步一步不落（装什么、准备哪些数据、"
      + "清理什么）—— 少一步别人就重现不出来。",
  },
];

/*
 * MERGE_SECTIONS 删掉了（环 v3，2026-08-09）：合并是 git 动作不是对抗阶段 ——
 * 守卫是 pre-push 印章，动作归人、在自由终端做。退休阶段不留模板。
 */

/**
 * Build 的六节 —— **施工报告**，不是代码本身。
 *
 * ## 它凭什么有模板
 *
 * 这个模块原来写着「Build 交的是 commit，套模板等于造一份永远缺齐所有节的产出」。
 * **那句话当时是对的，因为 `round-turn-runner` 不给它文档路径** —— 而那是把
 * 「交 commit」和「交文档」绑成互斥的一处混淆，2026-08-06 解开了。
 *
 * 现在 Build 交两样：代码进 commit，**一份说清「改了什么、跑过没有」的报告进模板**。
 * 报告才是它的可离散化表面，模板挂在报告上，代码那半归 diff 闸门和 rubric 管。
 *
 * ## `tests` 那一节是承重的
 *
 * 用户拍板「Build 不许自己写测试」（BACKLOG §8.4）。环 v3 又加了一层：**两轨
 * 互盲** —— Build 连读测试都不许（看得见就会向测试过拟合），跑测试整个是 QA
 * 对撞的事。这一节因此只收「改动本身的验证」；它自己写了测试的话，这一节
 * 要么空、要么露馅，而轮末的 diff 闸门是第二道（机械的那道）。
 */
const BUILD_SECTIONS: readonly TemplateSection[] = [
  {
    key: "did",
    title: "这一轮改了什么",
    asks: "按 BuildPlan 的哪几步做的，实际动了哪些文件。不要复述 BuildPlan。**文件"
      + "逐个列出路径**，每个后面写清在里面加了改了哪个函数、哪个导出、哪一段；再写出"
      + "它对应 BuildPlan 的第几步（写编号就够了，那一步的内容不用抄过来）——"
      + "「重构了配置模块」没说出一个文件。",
  },
  {
    key: "tests",
    title: "自己跑了什么来验证",
    asks: "跑的是改动本身（编译、启动、走一遍改的路径），命令和输出贴出来。"
      + "**这一阶段不读、不写、不跑任何测试代码** —— 测试是另一条轨（Test）的活儿，"
      + "跑起来对撞是 QA 的事。需要新用例就说明缺哪一条，写进「没做完的」。"
      + "命令原样贴（连同在哪个目录下跑的），输出贴出**判它成了的那几行原文**，"
      + "不是一句「编译通过」。",
  },
  {
    key: "deviation",
    title: "和 BuildPlan 不一样的地方",
    asks: "多做的、少做的、换了做法的，逐条写为什么。一条都没有就明写「没有」。每条要"
      + "指到 BuildPlan 的第几步和实际动的哪个文件，并把两边都写出来：计划说的是什么、"
      + "实际做的是什么 ——「做了些调整」既没指到步，也没说改成了什么。",
  },
  {
    key: "decisions",
    title: "不明显的决定",
    asks: "代码里看不出为什么的那几处，在这儿说清。**每处指到文件和函数名**，看的人"
      + "能直接翻到那儿；理由要落到一个具体的后果或另一种做法上（不这么做会怎样、当时"
      + "另一种写法为什么不行）—— 落不到这两样上的话，「不明显的决定」和一句复述没区别。",
  },
  {
    key: "risk",
    title: "这一轮最可能出问题的一处",
    asks: "指名一处，写为什么是它。并列罗列多项不算回答了这一节。**指到文件和函数名**，"
      + "并写出它真出问题时看到的是什么（哪条路径会走错、报出来的会是什么）——"
      + "「并发处理有风险」没有指到任何一处。",
  },
  {
    key: "leftover",
    title: "没做完的",
    asks: "知道还差什么、留给谁。没有就明写「没有」。每条写出差的是哪个文件的哪一处、"
      + "留给哪个阶段 ——「还有一些边界情况没处理」既没说哪个文件，也没说留给谁，"
      + "下一个阶段接不住。",
  },
];

/*
 * RETRO_SECTIONS 删掉了（环 v3，2026-08-09）：复盘是自审，内容不许承重 ——
 * 真复盘是 gap 账本和判例修 rubric 的回路。退休阶段不留模板。
 */
/**
 * Test 阶段的报告模板（环 v3 从 TestPlan 拆出，2026-08-09）。
 *
 * 最小可跑的四节 —— 批 2 拿真机的第一份报告回来定稿。落点那一节是批 4 工作区
 * 围栏的取数口：Test 轨声明的落点文件，就是 Build 提交前挡门要查的交集。
 */
const TEST_SECTIONS: readonly TemplateSection[] = [
  {
    key: "written",
    title: "这一轮写了哪些测试",
    asks: "逐个用例：TestPlan 里的哪一条、落在哪个文件（路径写全）。"
      + "方案里有而这儿没有的，写进「没做完的」，不许静默少。",
  },
  {
    key: "mapping",
    title: "和方案的对应",
    asks: "TestPlan 的每条用例 id 对到测试代码里的具体测试名。对不上的说为什么。",
  },
  {
    key: "selfrun",
    title: "自己跑了什么来验证",
    asks: "测试代码本身跑得动吗（语法、依赖、fixture）。**不许拿实现来跑** ——"
      + "测试对不对得上实现是 QA 对撞的事，这儿只证测试自己站得住。",
  },
  {
    key: "unfinished",
    title: "没做完的",
    asks: "没写的用例、写了但自己都觉得站不住的 —— 一条条列，空着要写「无」。",
  },
];

/*
 * 退休阶段（TechSpec 08-08；Plan / Review / Merge / Retro 环 v3）**不再给模板**。
 *
 * `templateFor` 返回 null = 这个阶段的红方不受模板约束、缺节也不挡门
 * （`domain/phase.ts` 的 `RETIRED_PHASES` 说了为什么名字还留着）。没有任何
 * Change 会再走到它们，所以那些格就该是空的：留一份模板在这儿，下一个人会
 * 以为它还在用。旧的 `*_SECTIONS` 常量还在上面，历史读产出时对得上节标题。
 */
const TEMPLATES: Partial<Readonly<Record<Phase, readonly TemplateSection[]>>> = {
  PRD: PRD_SECTIONS,
  Spec: SPEC_SECTIONS,
  Arch: ARCH_SECTIONS,
  // BuildPlan 顶替退休的 Plan —— 同一份模板，阶段换了名字。
  BuildPlan: PLAN_SECTIONS,
  TestPlan: TESTPLAN_SECTIONS,
  Build: BUILD_SECTIONS,
  Test: TEST_SECTIONS,
  QA: QA_SECTIONS,
};

/** 这个阶段的模板，没有就 `null`。 */
export function templateFor(phase: Phase): readonly TemplateSection[] | null {
  return TEMPLATES[phase] ?? null;
}

/** 印给红方看的那份。 */
export function renderTemplate(sections: readonly TemplateSection[]): string {
  return sections.map((each) => `## ${each.title}\n${each.asks}`).join("\n\n");
}

/**
 * 哪几节没写。返回缺掉的 `key`，按模板顺序。
 *
 * ## 宽的是识别，严的是数数
 *
 * 和 `readBlueRubricAnswers` 同一条：少认出一节会把一份好产出判成不合格，**那和判错
 * 一样糟**。所以井号几个、标题前后的空格、行尾空格一律不计较。
 *
 * 但**标题要逐字相等，不是包含** —— 「## 验收标准怎么写」不是「验收标准」那一节。
 * 松到按包含认，两个标题会互相认领，而错认比没认出来更难查。
 *
 * ## 标题底下空着也算缺
 *
 * 填了标题不等于回答了。只查「到下一个标题之间有没有非空的一行」—— 再深就变成
 * 判内容质量了，而那是 rubric 的活儿，不是这儿的。
 */
export function missingSections(
  markdown: string,
  sections: readonly TemplateSection[],
): readonly string[] {
  const lines = markdown.split("\n");
  const titleOf = (line: string): string | null =>
    /^#{1,6}\s+(.*?)\s*$/.exec(line)?.[1] ?? null;

  const missing: string[] = [];
  for (const section of sections) {
    const at = lines.findIndex((line) => titleOf(line) === section.title);
    if (at < 0) {
      missing.push(section.key);
      continue;
    }
    const rest = lines.slice(at + 1);
    const until = rest.findIndex((line) => titleOf(line) !== null);
    const body = (until < 0 ? rest : rest.slice(0, until)).join("").trim();
    if (body === "") missing.push(section.key);
  }
  return missing;
}
