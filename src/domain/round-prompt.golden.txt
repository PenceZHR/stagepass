########## PRD ##########
你是本轮的裁判。阶段：PRD，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）
   下面这份模板**原样转达给正方**，一个字都不要改 ——它必须照这个模板写，每一节都要有，标题原样用：
## 要解决谁的什么问题
谁在用、他今天怎么受阻、不解决会怎样。不要写要做什么功能。**用的人要点得出名字**：哪一类人、在什么场景下，不是一句「用户」；怎么受阻要写出他今天实际走的那几步、卡在哪一步，不是「体验不好」。

## 做完之后什么变了
可观察的结果，不是功能清单。每条写清谁、在什么时候、看到什么不一样了；和今天比是从什么变成什么 —— 能写出数的地方写数，「更快」「更稳」后面必须跟着从多少到多少。

## 验收标准
每条可观察、可测量 —— 到「一个称职的实施者照着做不会做错」为止，不要求每个词都能唯一裁定。但**下限是三样都写出来**：在什么条件下、量的是哪个指标、到什么数（或什么状态算通过）。这两句不冲突：前一句说的是不必再往下抠词，这一句说的是这三样一样都不能少。

## 这次不做什么
明确排除掉的。每条点到具体的那件事，不是一个大类 ——「暂不做性能优化」不算一条，要写出不做的是哪个场景下的哪件事，以及不做它谁会受影响。

## 前提
至少一条会让整个方案不成立的前提，以及怎么判它成不成立。判法要写成**一个做得出来的动作加一条明确的分界**：谁去做什么、看到什么就算它不成立 ——「需要进一步验证」不是判法。

## 已经定死的约束
人已经拍死、这次不容商量的：技术栈、平台、必须兼容什么、不许动什么。**只转述，不新定** —— 这里写的每一条都要说得出是谁定的。没有就写「没有」。每条要点到名字和版本：哪个框架、哪个版本、哪个平台、哪个不许动的文件或接口——「沿用现有技术栈」没说出任何一个名字，下游照样得猜。

## 留给下游决定的
有意留给下游去定的，写明留给哪个阶段。架构、模块划分、接口、测试用例、实现步骤都不在 PRD 里定；**已经被人定死的不写这儿，写上一节**。每条除了写留给哪个阶段，还要把**要它答的那个问题**写出来 ——「架构留给 Arch」不算一条，「A 和 B 要不要合成一个模块，留给 Arch」才是。
   模板之外不要另起小节。**架构、技术栈、模块划分、接口、测试用例、实现步骤都不在这个阶段定** —— 需要提到就写进「留给下游决定的」那一节。

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，**照着那份逐条判定的标准**，看它每一节写得够不够格。这个阶段你只判那些标准，不另外提问题。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/PRD-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   读正方报出来的那份产出文件本身（它交的是路径，正文在文件里），基于它提出问题。除那份产出之外不要读仓库的其他内容、不要自己动手修。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path you wrote your opinion to>"], "overall": "<one sentence on whether this round is good enough, and why>"}
   **不要另外列问题清单** —— 这个阶段你的判断全部走下面那份逐条判定。模板之外的事（架构、技术栈、实现细节、测试用例）不归这个阶段管，不要提。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## Spec ##########
你是本轮的裁判。阶段：Spec，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）
   下面这份模板**原样转达给正方**，一个字都不要改 ——它必须照这个模板写，每一节都要有，标题原样用：
## 这份 Spec 覆盖 PRD 的哪几条
逐条对应回 PRD，写清哪条验收标准由这里的哪几段行为满足。没有凭空多出来的。**两边都要点到名**：PRD 那一条写出它的编号或原话，这里这几条写出它们的名字或编号 ——「已覆盖 PRD 的验收标准」不是一条对应关系，它两边都没点到。

## 行为
每条行为的正常路径：给什么、做什么、出什么。**每条行为要有一个名字或编号**，下游靠它引用；输入逐项列（每项叫什么、什么含义、必填还是可选、取什么值算合法），输出写出具体是什么 ——「处理请求并返回结果」一项都没交代。说的是**行为层面的输入输出，不是函数签名** —— 那些属于 Arch。

## 边界和出错
每条行为在边界上和出错时的表现。没有「其余情况未定义」这种写法。每条要写出**触发条件**（哪个输入、取什么值、在什么状态下）和**当时的具体表现**（出什么、报哪一种错、做了一半的怎么办）——「报错」不是表现，它没说是哪一种错。

## 用词
这份文档里的关键词各是什么意思。和上游一致，同一个概念不发明第二个名字。每个词要说得出**什么算、什么不算**，而不是把这个词换个说法再说一遍；和 PRD 用词不同的，写出它对的是 PRD 里的哪个词。

## 留给下游决定的
有意不在 Spec 定的，写明留给哪个阶段。数据怎么存、模块怎么划分、用什么技术都不在 Spec 里定 —— 需要提到就写在这一节。每条要把**要下游答的那个问题**写出来 ——「存储方案留给 Arch」不算，「这份状态要不要落盘、落在哪儿，留给 Arch」才是。
   模板之外不要另起小节。**架构、技术栈、模块划分、接口、测试用例、实现步骤都不在这个阶段定** —— 需要提到就写进「留给下游决定的」那一节。

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，**照着那份逐条判定的标准**，看它每一节写得够不够格。这个阶段你只判那些标准，不另外提问题。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/Spec-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   读正方报出来的那份产出文件本身（它交的是路径，正文在文件里），基于它提出问题。除那份产出之外不要读仓库的其他内容、不要自己动手修。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path you wrote your opinion to>"], "overall": "<one sentence on whether this round is good enough, and why>"}
   **不要另外列问题清单** —— 这个阶段你的判断全部走下面那份逐条判定。模板之外的事（架构、技术栈、实现细节、测试用例）不归这个阶段管，不要提。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## Arch ##########
你是本轮的裁判。阶段：Arch，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）
   下面这份模板**原样转达给正方**，一个字都不要改 ——它必须照这个模板写，每一节都要有，标题原样用：
## 这次动哪几个模块
已有的哪几个、新增哪几个。**每个模块写出它的目录或文件路径**，不能只给一个概念名字。每个新增的写清它为什么不能并进已有的。

## 文件与函数清单
**逐个文件列**：路径、这个文件负责什么、它导出哪几个函数或类型（**把名字写出来**）、谁调它们。新建的和要改的都列，改的写清改哪几个导出。这一节不许出现「等实现时再定」—— 定不下来的函数说明模块还没划清。

## 数据怎么存、状态怎么迁移
**每个数据结构写出它的字段名和类型**；哪些是权威、哪些是镜像；状态之间怎么走（写出状态名和触发它的那个函数）。没有持久化数据时明写「没有」，而不是跳过这一节。

## 接口与契约
**逐个写出上一节点过名的函数的签名**：参数名和类型、返回什么、出错时返回什么或抛什么。跨模块调用的每一条都要有；模块内部的私有函数不用写。「返回一个结果对象」这种不算 —— 写出它有哪几个字段。

## 新增了哪些依赖边
**每条写成「哪个文件 import 哪个文件」**，再写为什么非加不可 ——说得出少了它哪个行为做不成。没有新增时明写「没有」：这一节是 Build 反馈回流的落点，空着和「没有」是两回事。

## 边界划在哪
每个被动到的模块对外露什么、不露什么 —— **露的那些要点名到导出的函数或类型**，不能只说「只露出配置能力」。用 Spec 的词，不发明新名字。

## 想过但没选的划法，和选了的代价
至少一个没选的划法，写为什么没选（没有备选说明还没想）；**以及选了的这一套要付的代价** —— 一个只有好处的选择说明代价还没被看见。

## 最可能出错的一处
**指名一处**，写为什么是它、错了会怎样。并列罗列多项风险不算回答了这一节 —— 那等于没有判断。

## 对得回 Spec
这里的每一组决定对应 Spec 的哪一条。**没有引入 Spec 里不存在的行为**；真需要新行为，说明它是 Spec 漏了（那要打回 Spec），不是在这里补。

## 留给下游决定的
有意留给 Plan 或 Build 的，写明留给哪个阶段、以及为什么现在定不了。**「实现细节」不是理由** —— 函数名、签名、数据结构都属于这一份，推给下游的只能是「怎么排步骤」和「具体怎么写代码」。

## 机器可读的架构图（文件，不是文档里的一节）
把上面各节的结论**另存成一个 JSON 文件**：`docs/stagepass/<本 Change 的 id>/arch.graph.json`。固定形状，三个键都必须有：
`concepts`（数组，每项 `{"id", "name"}` —— 这次改动涉及的概念，id 用稳定的短英文）；
`relations`（数组，每项 `{"from", "to", "why"}` —— from/to 是概念 id，why 一句话说这条关系是什么）；
`serves`（对象，`{"仓库相对文件路径": ["概念 id"]}` —— **「文件与函数清单」那一节点过名的每个文件都要在这里认领它服务的概念**，说不出服务谁的文件说明它不该出现在清单里）。
这份文件会被机器对账（规划 vs 真实代码）并画在图谱上 —— 图没法宽泛，写不出来的关系就是还没想清的关系。
   模板之外不要另起小节。**数据存储、接口契约、测试用例、实现步骤都不在这个阶段定** —— 需要提到就写明留给哪个下游阶段（多半是 TechSpec），不展开。

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，**照着那份逐条判定的标准**，看它每一节写得够不够格。这个阶段你只判那些标准，不另外提问题。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/Arch-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   读正方报出来的那份产出文件本身（它交的是路径，正文在文件里），基于它提出问题。除那份产出之外不要读仓库的其他内容、不要自己动手修。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path you wrote your opinion to>"], "overall": "<one sentence on whether this round is good enough, and why>"}
   **不要另外列问题清单** —— 这个阶段你的判断全部走下面那份逐条判定。模板之外的事（数据存储、接口契约、测试用例、实现步骤）不归这个阶段管，不要提。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## TechSpec ##########
你是本轮的裁判。阶段：TechSpec，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，**照着那份逐条判定的标准**，看它每一节写得够不够格。这个阶段你只判那些标准，不另外提问题。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/TechSpec-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   读正方报出来的那份产出文件本身（它交的是路径，正文在文件里），基于它提出问题。除那份产出之外不要读仓库的其他内容、不要自己动手修。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path you wrote your opinion to>"], "overall": "<one sentence on whether this round is good enough, and why>"}
   **不要另外列问题清单** —— 这个阶段你的判断全部走下面那份逐条判定。模板之外的事（架构、技术栈、实现细节、测试用例）不归这个阶段管，不要提。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## Plan ##########
你是本轮的裁判。阶段：Plan，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，**照着那份逐条判定的标准**，看它每一节写得够不够格。这个阶段你只判那些标准，不另外提问题。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/Plan-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   读正方报出来的那份产出文件本身（它交的是路径，正文在文件里），基于它提出问题。除那份产出之外不要读仓库的其他内容、不要自己动手修。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path you wrote your opinion to>"], "overall": "<one sentence on whether this round is good enough, and why>"}
   **不要另外列问题清单** —— 这个阶段你的判断全部走下面那份逐条判定。模板之外的事（架构、技术栈、实现细节、测试用例）不归这个阶段管，不要提。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## BuildPlan ##########
你是本轮的裁判。阶段：BuildPlan，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）
   下面这份模板**原样转达给正方**，一个字都不要改 ——它必须照这个模板写，每一节都要有，标题原样用：
## 步骤
每一步一个能独立交付的东西。一步做完就能看出对不对。**每一步要写出它动哪几个文件**（路径写全，新建的写清准备放在哪儿），以及在每个文件里加什么、改什么（函数名、导出名、配置项名）——「实现 X 模块」不是一步，它没说出一个文件。确实不动文件的步骤（比如只跑一次验证）写清它跑什么、看什么。

## 依赖和顺序
哪几步必须先后，为什么。没有两步在改同一处却没定顺序。每条依赖写成「哪一步在哪一步之前」（用步骤号），理由要点到**它们共用的那个东西**：哪个文件、哪个函数、哪张表 ——「有依赖关系」不是理由，它没说共用的是什么。

## 每一步怎么验
每一步各自的判据，不要等到最后才知道对不对。每一步写出**跑哪条命令**（原样写出来）、**看到什么算过**；不是命令能验的，写出去看哪个文件的哪一处、看到什么算过 ——「跑一下测试」「确认功能正常」都不是判据，它们没有对象。

## 出问题怎么退
实施到一半失败了怎么退回去。只写停止条件不算回答了这一节。要写出**做哪几个动作**：退哪个 commit 或哪几个文件、已经改掉的数据要不要反着改回来、退完之后看什么确认退干净了 —— 少了最后一样，退没退成没人知道。

## 哪一步风险最高
指名一步，以及为什么先做或后做它。指的是**第几步**（写出步骤号或标题），理由落到那一步动的那个具体东西上：哪个文件、哪条依赖、哪个还没验过的假设 ——「涉及核心逻辑」没有指到任何一处。
   模板之外不要另起小节。**架构、技术栈、模块划分、接口、测试用例、实现步骤都不在这个阶段定** —— 需要提到就写进「留给下游决定的」那一节。

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，**照着那份逐条判定的标准**，看它每一节写得够不够格。这个阶段你只判那些标准，不另外提问题。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/BuildPlan-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   读正方报出来的那份产出文件本身（它交的是路径，正文在文件里），基于它提出问题。除那份产出之外不要读仓库的其他内容、不要自己动手修。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path you wrote your opinion to>"], "overall": "<one sentence on whether this round is good enough, and why>"}
   **不要另外列问题清单** —— 这个阶段你的判断全部走下面那份逐条判定。模板之外的事（架构、技术栈、实现细节、测试用例）不归这个阶段管，不要提。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## TestPlan ##########
你是本轮的裁判。阶段：TestPlan，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）
   下面这份模板**原样转达给正方**，一个字都不要改 ——它必须照这个模板写，每一节都要有，标题原样用：
## 验收标准到用例的映射
上游每条验收标准对应这里的哪几个用例。逐条列，不留空。**两边都点到名**：验收标准写出它的编号或原话，用例写出它的 id ——「已全部覆盖」不是一条映射，它一个 id 都没写出来。

## 用例
每个用例：给什么输入、在什么前置状态下、期望什么输出。**每个用例要有一个 id，还要声明它将落在哪个测试文件的哪个用例名上** —— 写测试的是 Test 阶段，它照这份声明落笔；跑测试的是 QA，按 id 单独跑它。落点声明同时是工作区围栏的边界：施工轨不碰这些文件。输入写出实际的值，不是「一个合法的输入」；前置状态写出具体有哪些数据、系统在哪个状态；期望输出写到能逐字比对 ——「返回成功」没说要比对什么。

## 失败长什么样
不只写成功。每个用例失败时看到的是什么，怎么和「测试自己写错了」区分开。**失败要写到能对上号**：报出来的是哪一句、断言差在哪两个值之间；区分方法要写成一个做得出来的动作（跑哪条命令、改哪一处再跑一次看什么变了），不是「检查测试代码」。

## 哪些必须通过
区分「必须通过」和「知道会失败但先记着」，后者写明为什么先记着。**两边都按用例 id 逐个列**，不是「核心用例都要过」；先记着的每条还要写出什么条件下它会被解除 —— 没有解除条件的「先记着」就是永远记着。

## 怎么跑
命令、环境、前置条件。**跑的人不是你** —— QA 在两轨都交工之后照这一节执行，所以它必须细到不在场的人照着能跑。没有「跑一遍看看」这种写法。命令**原样写出来能直接粘**（连同在哪个目录下跑），环境写到版本号（运行时、包管理器、要不要先装依赖），并且写出**只跑其中一条用例**的命令 —— QA 是按 id 复核的，它得单独跑得动某一条。
   模板之外不要另起小节。**架构、技术栈、模块划分、接口、测试用例、实现步骤都不在这个阶段定** —— 需要提到就写进「留给下游决定的」那一节。

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，**照着那份逐条判定的标准**，看它每一节写得够不够格。这个阶段你只判那些标准，不另外提问题。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/TestPlan-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   读正方报出来的那份产出文件本身（它交的是路径，正文在文件里），基于它提出问题。除那份产出之外不要读仓库的其他内容、不要自己动手修。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path you wrote your opinion to>"], "overall": "<one sentence on whether this round is good enough, and why>"}
   **不要另外列问题清单** —— 这个阶段你的判断全部走下面那份逐条判定。模板之外的事（架构、技术栈、实现细节、测试用例）不归这个阶段管，不要提。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## Build ##########
你是本轮的裁判。阶段：Build，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）
   下面这份模板**原样转达给正方**，一个字都不要改 ——它必须照这个模板写，每一节都要有，标题原样用：
## 这一轮改了什么
按 BuildPlan 的哪几步做的，实际动了哪些文件。不要复述 BuildPlan。**文件逐个列出路径**，每个后面写清在里面加了改了哪个函数、哪个导出、哪一段；再写出它对应 BuildPlan 的第几步（写编号就够了，那一步的内容不用抄过来）——「重构了配置模块」没说出一个文件。

## 自己跑了什么来验证
跑的是改动本身（编译、启动、走一遍改的路径），命令和输出贴出来。**这一阶段不读、不写、不跑任何测试代码** —— 测试是另一条轨（Test）的活儿，跑起来对撞是 QA 的事。需要新用例就说明缺哪一条，写进「没做完的」。命令原样贴（连同在哪个目录下跑的），输出贴出**判它成了的那几行原文**，不是一句「编译通过」。

## 和 BuildPlan 不一样的地方
多做的、少做的、换了做法的，逐条写为什么。一条都没有就明写「没有」。每条要指到 BuildPlan 的第几步和实际动的哪个文件，并把两边都写出来：计划说的是什么、实际做的是什么 ——「做了些调整」既没指到步，也没说改成了什么。

## 不明显的决定
代码里看不出为什么的那几处，在这儿说清。**每处指到文件和函数名**，看的人能直接翻到那儿；理由要落到一个具体的后果或另一种做法上（不这么做会怎样、当时另一种写法为什么不行）—— 落不到这两样上的话，「不明显的决定」和一句复述没区别。

## 这一轮最可能出问题的一处
指名一处，写为什么是它。并列罗列多项不算回答了这一节。**指到文件和函数名**，并写出它真出问题时看到的是什么（哪条路径会走错、报出来的会是什么）——「并发处理有风险」没有指到任何一处。

## 没做完的
知道还差什么、留给谁。没有就明写「没有」。每条写出差的是哪个文件的哪一处、留给哪个阶段 ——「还有一些边界情况没处理」既没说哪个文件，也没说留给谁，下一个阶段接不住。
   模板之外不要另起小节。**架构、技术栈、模块划分、接口、测试用例、实现步骤都不在这个阶段定** —— 需要提到就写进「留给下游决定的」那一节。

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方这一轮的施工报告和它改的代码，**照着那份逐条判定的标准**，看报告每一节写得够不够格、代码对不对得上它说的。这个阶段你只判那些标准，不另外提问题。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/Build-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   可以读这一轮改动涉及的文件、它们的直接调用方，以及任务里列出的那几份已批准上游产物（要判它有没有做到要求，就得对着要求看）—— 只读这些，不要把整个仓库读一遍。不要自己执行任何东西，也不要动手修代码。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path you wrote your opinion to>"], "overall": "<one sentence on whether this round is good enough, and why>"}
   **不要另外列问题清单** —— 这个阶段你的判断全部走下面那份逐条判定。模板之外的事（架构、技术栈、实现细节、测试用例）不归这个阶段管，不要提。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## Test ##########
你是本轮的裁判。阶段：Test，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）
   下面这份模板**原样转达给正方**，一个字都不要改 ——它必须照这个模板写，每一节都要有，标题原样用：
## 这一轮写了哪些测试
逐个用例：TestPlan 里的哪一条、落在哪个文件（路径写全）。方案里有而这儿没有的，写进「没做完的」，不许静默少。

## 和方案的对应
TestPlan 的每条用例 id 对到测试代码里的具体测试名。对不上的说为什么。

## 自己跑了什么来验证
测试代码本身跑得动吗（语法、依赖、fixture）。**不许拿实现来跑** ——测试对不对得上实现是 QA 对撞的事，这儿只证测试自己站得住。

## 没做完的
没写的用例、写了但自己都觉得站不住的 —— 一条条列，空着要写「无」。
   模板之外不要另起小节。**架构、技术栈、模块划分、接口、测试用例、实现步骤都不在这个阶段定** —— 需要提到就写进「留给下游决定的」那一节。

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方这一轮的测试报告和它写的测试代码，**照着那份逐条判定的标准**，看报告每一节写得够不够格、测试对不对得上 TestPlan 说的。这个阶段你只判那些标准，不另外提问题。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/Test-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   可以读这一轮新增或改动的测试文件，以及任务里列出的那几份已批准上游产物（要判它有没有覆盖方案的要求，就得对着方案看）—— 只读这些，**不要去读被测的实现代码**：测试对不对得上实现是 QA 对撞的事，这儿看得见实现，测试就不再独立。不要自己执行任何东西，也不要动手修。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path you wrote your opinion to>"], "overall": "<one sentence on whether this round is good enough, and why>"}
   **不要另外列问题清单** —— 这个阶段你的判断全部走下面那份逐条判定。模板之外的事（架构、技术栈、实现细节、测试用例）不归这个阶段管，不要提。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## Review ##########
你是本轮的裁判。阶段：Review，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   每个问题一个稳定 id，**必须以 `RV-` 开头**（例如 `RV-NULL-DEREF-1`）—— 另一边用的是别的前缀，撞了会丢一条。同一个问题在后续轮次要用同一个 id。
   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，找出其中的遗漏、冲突与不可验证之处。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/Review-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   可以读被审的那个 commit 涉及的文件，以及它们的直接调用方 —— 自己去看，不要只凭正方的报告下结论。不要自己执行任何东西，也不要动手修：跑起来验是 QA 的活儿。
   每个问题一个稳定 id，**必须以 `RVB-` 开头**（例如 `RVB-NULL-DEREF-1`）—— 另一边用的是别的前缀，撞了会丢一条。同一个问题在后续轮次要用同一个 id。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md
   它找出的每个问题都要放进 blockers。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## Fix ##########
你是本轮的裁判。阶段：Fix，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，找出其中的遗漏、冲突与不可验证之处。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/Fix-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   可以读这一轮改动涉及的文件、它们的直接调用方，以及任务里列出的那几份已批准上游产物（要判它有没有做到要求，就得对着要求看）—— 只读这些，不要把整个仓库读一遍。不要自己执行任何东西，也不要动手修：跑没跑过看正方交出来的运行证据。
   每个问题一个稳定 id（例如 SPEC-SCOPE-1），同一个问题在后续轮次要用同一个 id。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md
   它找出的每个问题都要放进 blockers。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## QA ##########
你是本轮的裁判。阶段：QA，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）
   下面这份模板**原样转达给正方**，一个字都不要改 ——它必须照这个模板写，每一节都要有，标题原样用：
## 测的是哪一个 commit
写出 sha —— **两条轨各一个**（Build 的代码 commit、Test 的测试 commit），不要笼统地说「当前代码」。**sha 至少写到前 12 位**，再写出你是在哪个目录、哪个分支、什么环境下测的 —— 反方要在同一处跑一遍核对你说的。

## 逐条对照上游的结果
对着 Spec 和 Arch 一条条看 Build 的代码，不是通读一遍谈感受。每条写成三样：**上游那一条**（编号或原话）、**代码里落在哪个文件的哪个函数**、**对上了还是没对上** ——「整体符合设计」这三样一样都没有。

## 读出来的问题
静态审查发现的缺陷，每条指明文件和位置，看的人能直接翻到那儿。没有就明写没有。**位置要到文件路径加函数名或行号**，还要写出这条在什么输入、什么状态下会真的出问题 ——「错误处理不够完善」既不是位置，也不是条件。

## 按 TestPlan 逐条执行的结果
一条都不跳。跳过的写明为什么。**按 TestPlan 的用例 id 逐条列**，每条写清跑的是哪条命令、结果是过还是没过 ——「全部用例执行通过」不是逐条，它一个 id 都没写出来。

## 失败的用例
实际输出是什么、它来自哪一条用例。不只写「没过」。**输出贴原文**（报错那几行、断言里实际值和期望值各是什么），并写出它的用例 id 和所在的测试文件 ——转述一遍，下一轮就没法拿它比对了。

## 有没有让别处退化
这一轮的改动之外的地方，查过没有、结果是什么。**写出查的是哪几处**：跑了哪条命令、覆盖了哪些用例或哪些文件，结果各是什么 ——「未发现回归」没说查过什么，和没查看起来一模一样。

## 变异检验：测试自己站不站得住
两个方向都要做、都贴证据：**还原必红** —— 把 Build 的改动临时还原（`git stash` / `git revert -n`，做完恢复原样），必过用例必须变红，全绿说明测试没测到这次改动；**no-op 仍绿** —— 给实现打一个不改行为的变异（比如改一句注释、换一个内部变量名），用例必须仍然全绿，红了说明测试钉的是源码文本不是行为。每个方向写出动了什么、跑了哪条命令、看到什么 —— 两样都是对**测试**的攻击，串过供的一致什么都不是，这两下就是拆串供的。

## 怎么重现
跑的命令和环境写下来，别人照着能重现。**命令原样能粘**（连同在哪个目录下跑），环境写到版本号，从零开始要做的那几步一步不落（装什么、准备哪些数据、清理什么）—— 少一步别人就重现不出来。
   模板之外不要另起小节。**架构、技术栈、模块划分、接口、测试用例、实现步骤都不在这个阶段定** —— 需要提到就写进「留给下游决定的」那一节。

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   每个问题一个稳定 id，**必须以 `QA-` 开头**（例如 `QA-NULL-DEREF-1`）—— 另一边用的是别的前缀，撞了会丢一条。同一个问题在后续轮次要用同一个 id。
   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，找出其中的遗漏、冲突与不可验证之处。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/QA-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   可以读代码，也**可以自己跑一遍**验证正方报的结果 —— 这是唯一一个活儿本身就是执行的阶段，一个跑不了东西的反方核对不了一份「我跑了、结果是这样」的报告。不要动手改代码：这一轮的活儿是验，不是修。
   每个问题一个稳定 id，**必须以 `QAB-` 开头**（例如 `QAB-NULL-DEREF-1`）—— 另一边用的是别的前缀，撞了会丢一条。同一个问题在后续轮次要用同一个 id。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md
   它找出的每个问题都要放进 blockers。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## Merge ##########
你是本轮的裁判。阶段：Merge，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   每个问题一个稳定 id（例如 SPEC-SCOPE-1），同一个问题在后续轮次要用同一个 id。
   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，找出其中的遗漏、冲突与不可验证之处。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/Merge-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   可以读这一次的全部 commit（`git log` 里属于这个 Change 的那些）以及它们改到的文件 —— 交付说明里「改了什么、对谁有影响、能不能回滚」全是对代码的断言，不自己看就只能跟着正方点头。不要自己执行任何东西，也不要动手改。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md
   它找出的每个问题都要放进 blockers。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。

########## Retro ##########
你是本轮的裁判。阶段：Retro，第 2 轮。

派生两个子 Agent，一个当正方、一个当反方。**你手上哪个 spawn 工具都行**，
叫什么名字、什么路径都不要紧。

**必须先派正方，等它跑完再派反方，不要并行。** 两个理由：反方要拿到正方的产出
才能开始审，并行的话它会对着空气写意见；而且 StagePass 就是按这个先后认
谁是正方、谁是反方的 —— 顺序反了，两边的话会被记到对方头上。

1. 正方。**下面这段任务原样转达给正方，一个字都不要改** ——「人要的是这些」那几行是人自己答的需求，改写或省略它，正方就只能凭空编。
（这一阶段的任务书）

   人明确要求这一轮处理的（不许当成建议）：
   - HUMAN-1 [P1] 人明确要求的那一条
     人说：人对这条说的话

   上一轮报出来的问题：
   - SPEC-SCOPE-1 [P0] 模型报出来的那一条
   以上每一条都要处理 —— 改掉它，或者在产出里写清楚为什么它不成立。

   要求它按下面的格式作答：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path or id you produced>"], "blockers": [{"id": "...", "severity": "P0|P1|P2", "title": "...", "where": "...", "why": "...", "owner": null}]}
Report every problem you found as a blocker. An empty list means you found none.
Set "owner" only when the fix belongs to ANOTHER phase, and then it must be that phase's exact name; leave it null when the problem is this phase's own to fix.
   各字段什么意思在这个文件里，**原样转达这一行**，让它先读：/tmp/stagepass-round/result-contract-notes.md

2. 反方。任务：读正方产出，**照着那份逐条判定的标准**，看它每一节写得够不够格。这个阶段你只判那些标准，不另外提问题。
   意见写到仓库里这个路径（相对项目根）：docs/stagepass/CHG-1/Retro-r2-opposition.md —— 不要写到别的地方，也不要自己起名。
   读正方报出来的那份产出文件本身（它交的是路径，正文在文件里），基于它提出问题。除那份产出之外不要读仓库的其他内容、不要自己动手修。
   下面这段格式要求**原样转达给反方**，一个字都不要改：
Reply with one ```json block and nothing that contradicts it:
{"artifactIds": ["<path you wrote your opinion to>"], "overall": "<one sentence on whether this round is good enough, and why>"}
   **不要另外列问题清单** —— 这个阶段你的判断全部走下面那份逐条判定。模板之外的事（架构、技术栈、实现细节、测试用例）不归这个阶段管，不要提。
   再要它在同一个 json 块里多给一个 `overall` 字段：一句话说这一轮整体够不够格、为什么。这一句不挡任何东西，是写给人看的。

两个都做完之后，轮到你。**两件事**：

1. **逐条表态**，一共 2 条 —— 已经存在的问题，外加几条要你判的标准：

   要表态的全部列在这个文件里，**先读它**：/tmp/stagepass-round/worklist.md
   表态写进这个文件：/tmp/stagepass-round/worklist-answers.md
   那个文件一行一条，形如 `3: closed —— 依据…`：序号、那一条的选项、破折号后面写一句为什么。
   **可选项每条不一样**，印在清单里它自己那一条旁边 —— 不要跨条套用。
   序号 1 到 2，一条只答一次；**答不上的那几条留空就是了，不要瞎填**（留空 = 你认为它还成立）。
   序号之外不要写任何编号，也不要把这些表态写进 json。

   下面这份清单是给你看**来历和分量**的（谁提的、哪一条以人的话为准）——不要把它的编号抄进任何地方。

**人明确要求下一轮处理的（不许当成建议）：**
- HUMAN-1 [P1] 人明确要求的那一条
  人说：人对这条说的话

上面这几条**和上游那些已批准的文档冲突时，以人的话为准**。
但**必须把冲突写进产出**：说清楚顶掉了上游的哪一句、为什么 —— 人看到之后可以决定「打回上游」把那份文档改了，那才是根治。闷头照做，他不会知道自己正跟一份文档拧着，于是每一轮都要再说一遍。

之前轮次报出来的问题：
- SPEC-SCOPE-1 [P0] 模型报出来的那一条

2. 自己读一遍上面任务里列出来的那些输入、正方这一轮的产出、反方这一轮的意见，然后给一个结论：**还要不要再来一轮，为什么。**
   这是给人看的建议 —— 按不按由他决定，**你不需要考虑闸门**。
   结论要建立在你实际读过的东西上，不是复述正方或反方说过的话。

最后用一个 ```json 块给出结论，**里面只放这一样东西**：
{"conclusion": {"another_round": true | false, "reason": "<为什么>"}}
不要把逐条表态写进这个块 —— 那些走上面那份答案文件，写在这里不算数。

沉默等于仍然存在 —— 你没在答案文件里表过态的问题会继续挡住闸门。
关闭一个问题必须写清楚它为什么不再成立。